# MultimodalMamba
We propose MultimodalMamba, a hardware-efficient HAR architecture that integrates Dilated Hierarchical CNNs (DHCNN), Frequency Attention (FAM), and Mamba to jointly capture local, frequency, and global sequence features with high accuracy and low computational cost.
